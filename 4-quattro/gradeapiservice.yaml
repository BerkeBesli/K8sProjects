apiVersion: v1
kind: Service
metadata:
  name: gradeapiservice
  namespace: grades    #podun oluşturulacağı namespace belirtilir
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: grademission
  ports:
  - port: 3000
    targetPort: 3000
