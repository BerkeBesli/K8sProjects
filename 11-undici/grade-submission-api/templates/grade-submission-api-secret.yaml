apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.microservice.name }}-secret
  namespace: {{ .Values.microservice.namespace }}
type: Opaque
data:
  MONGODB_USER: 'YWRtaW4='   ## assumes this is base64 encoded, and interprets as admin
  MONGODB_PASSWORD: 'cGFzc3dvcmQxMjM=' ## assumes this is base64 encoded, and interprets as password123