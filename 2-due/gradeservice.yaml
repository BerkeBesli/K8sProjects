apiVersion: v1
kind: Service
metadata:
  name: gradeservice
spec:
  type: NodePort
  selector:
    app.kubernetes.io/instance: gradesub
  ports:
  - port: 5001
    targetPort: 5001
    nodePort: 32000