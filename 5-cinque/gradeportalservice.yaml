apiVersion: v1
kind: Service
metadata:
  name: gradesubmissionportal
spec:
  type: NodePort
  selector:
    app.kubernetes.io/instance: gradesubmissionportal
  ports:
  - port: 5001
    targetPort: 5001
    nodePort: 32000